/**
 * Index.tsx - Home Page Route Component
 * 
 * "/" path ရဲ့ page component။
 * HomePage component ကို render လုပ်ပေးတယ်။
 * 
 * Pages folder ထဲမှာ route-level component ထားပြီး
 * Components folder ထဲမှာ actual UI logic ထားတယ်။
 */

import HomePage from "@/components/HomePage";

const Index = () => {
  return <HomePage />;
};

export default Index;
